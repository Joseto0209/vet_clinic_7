<div class="d-flex justify-content-between align-items-center mb-4">
  <h1>Edit Veterinarian</h1>
  <%= link_to "← Back to Veterinarian", vet_path(@vet), class: "btn btn-outline-secondary btn-sm" %>
</div>

<div class="card shadow-sm">
  <div class="card-body">
    <%= render "form", vet: @vet %>
  </div>
</div>
