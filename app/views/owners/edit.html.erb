<div class="d-flex justify-content-between align-items-center mb-4">
  <h1>Edit Owner</h1>
  <%= link_to "← Back to Owner", owner_path(@owner), class: "btn btn-outline-secondary btn-sm" %>
</div>

<div class="card shadow-sm">
  <div class="card-body">
    <%= render "form", owner: @owner %>
  </div>
</div>
