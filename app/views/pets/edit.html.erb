<div class="d-flex justify-content-between align-items-center mb-4">
  <h1>Edit Pet</h1>
  <%= link_to "← Back to Pet", pet_path(@pet), class: "btn btn-outline-secondary btn-sm" %>
</div>

<div class="card shadow-sm">
  <div class="card-body">
    <%= render "form", pet: @pet %>
  </div>
</div>
